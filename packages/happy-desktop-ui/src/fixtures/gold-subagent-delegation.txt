Read-only delegation workflow completed with exactly three concurrent subagents and one follow-up. No files, commits, or branches changed.

- Replay flow: captured events enter rig-connect’s `ChatStore`, then `rigChatStoreCreate` projects them into the desktop store. The replay driver exposes that snapshot through a stable external-store wrapper. See [rigConversationReplayDriver.ts](/Users/demo/Happy/Projects/happy-desktop/packages/happy-desktop-ui/dev/replay/rigConversationReplayDriver.ts:69).
- Proposed delegated row: reuse the compact `SubagentRow` with status dot, task, model/elapsed/tokens, and bounded latest activity. Keep it passive and keyed by stable subagent ID. See [RigActivityPanel.tsx](/Users/demo/Happy/Projects/happy-desktop/packages/happy-desktop-ui/src/RigActivityPanel.tsx:142).
- Chat filtering: omit lifecycle notices and opaque encrypted-collaboration placeholders. For legacy plaintext envelopes, strip addressing/steering boilerplate and retain only the collaborator’s readable payload. See [rigInboundMessageProject.ts](/Users/demo/Happy/Projects/happy-desktop/packages/happy-desktop-state/src/happyAgent/rigInboundMessageProject.ts:11).
- Anchoring: `parentToolCallId` is a foreign key to the parent transcript tool’s `toolCallId`. A delegated-work block should attach to that exact tool entry, grouping children if necessary—not infer placement from timestamps, tool names, or subagent IDs.
- Recording gap: `compose-gold.mjs` overlays only `subagent_changed` frames. Its recorded `parentToolCallId` has no matching parent spawn-tool frame, so the current fixture cannot truthfully render an anchored delegated-work block. It needs either the original matching spawn frame or an explicit orphan fallback. See [compose-gold.mjs](/Users/demo/Happy/Projects/happy-desktop/scripts/conversation-replay/compose-gold.mjs:158).